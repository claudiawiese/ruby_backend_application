# frozen_string_literal: true

source 'https://rubygems.org'
group :test do
  gem 'rspec'
end

# possibles other gems:
# - zeitwerk for autoload
# - sqlite3 to better organize relational tables
# - possibly active record

# for tests:
# -factory_bot